\section{Feature Engineering and Data Decisions}
\label{sec:pipeline}

The feature engineering follows a clear order: reduce the data, define 
the target, select and encode features, decide about scaling, and finally 
add lag features after the first results showed that weather and calendar 
information alone was not enough.

\begin{table}[htbp]
\caption{Main preprocessing and representation decisions.}
\label{tab:preprocessing}
\centering
\scriptsize
\begin{tabularx}{\columnwidth}{p{0.38\columnwidth} X}
\toprule
\textbf{Decision} & \textbf{Implementation} \\
\midrule
Station-day aggregation & 
Group trips by \texttt{start\_station\_id} and date, then merge 
daily weather data. \\
\addlinespace
Date transformation & 
Replace raw date strings with \texttt{month}, \texttt{weekday}, 
\texttt{year}, and \texttt{time\_idx}. \\
\addlinespace
Leakage control & 
Remove member/casual and bike-type count columns from input features. \\
\addlinespace
Correlation reduction & 
Remove near-duplicate variables identified in Fig.~\ref{fig:corr}, 
such as \texttt{solarenergy} and \texttt{feelslike}. \\
\addlinespace
No PCA & 
Keep original features instead of principal components. \\
\addlinespace
One-hot station encoding & 
Encode \texttt{start\_station\_id} as 20 binary columns 
\cite{sklearn_ohe}. \\
\addlinespace
Selective scaling & 
Standardize inputs only for Linear, Ridge, Lasso, KNN, and 
Neural Network models. \\
\addlinespace
Lag features & 
Add \texttt{total\_rentals\_lag\_1} and 
\texttt{total\_rentals\_lag\_7} per station. \\
\addlinespace
Fair comparison & 
Filter both lag and no-lag variants to the same station-day keys. \\
\bottomrule
\end{tabularx}
\end{table}

\subsection{Station Identity and One-Hot Encoding}

The first version of our pipeline used \texttt{start\_station\_id} 
as a plain numeric column. This is wrong because station 31623 is not 
larger or more important than station 31245. Machine learning models 
that use distances, coefficients, or penalties would treat these numbers 
as meaningful quantities. We therefore replaced the ID column with 20 
binary one-hot columns, one for each station. This gives every model 
family a clean and comparable station representation.

\subsection{Scaling and PCA}

Scaling is applied only where the model needs it. KNN uses distances, 
regularized linear models use coefficient penalties, and neural networks 
use gradient-based optimization -- all of these are sensitive to the 
scale of the input values. Decision Tree, Random Forest, and Gradient 
Boosting split data by thresholds and are not affected by scale. We 
did not apply PCA because our feature set is already small and every 
feature has a clear meaning (temperature, weekday, station, lag demand). 
Replacing them with abstract components would make the results much 
harder to explain.

\subsection{Lag Features}

Lag features are always computed per station, not across all stations. 
A global shift would mix demand from different stations and produce 
incorrect information. The lag value for a given station-day is the 
same station's rental count from one day earlier (\texttt{lag\_1}) and 
from seven days earlier (\texttt{lag\_7}). Rows without exact lag 
history are removed. The same rows are also removed from the no-lag 
dataset so that both variants are evaluated on identical data. This 
step directly addresses the main weakness of the early models: they 
used weather and calendar information, but had no memory of recent 
station demand.

\subsection{Why Representation Came Before More Models}

Adding more algorithms does not help if the input data is weak. Our 
early results showed that the biggest problem was not the choice of 
algorithm, but the missing temporal memory. We therefore focused on 
improving the data representation first -- station encoding and lag 
features -- before comparing models under one consistent evaluation 
protocol. This follows a data-centric approach: better inputs lead to 
better models across the board.